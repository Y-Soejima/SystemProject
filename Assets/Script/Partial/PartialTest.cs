using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public partial class PartialTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Test1();
        Test2();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
